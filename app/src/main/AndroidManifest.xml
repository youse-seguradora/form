<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.youse.forms">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".samples.launcher.LauncherActivity"
            android:label="@string/title_activity_launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".samples.home.HomeActivity"
            android:label="@string/title_activity_home" />
        <activity
            android:name=".samples.form.FormLoginActivity"
            android:label="@string/title_activity_form" />
        <activity
            android:name=".samples.rx.RxLoginActivity"
            android:label="@string/title_activity_rx_form" />
        <activity
            android:name=".samples.livedata.LiveDataLoginActivity"
            android:label="@string/title_activity_live_data" />
        <activity
            android:name=".samples.registration.RegistrationActivity"
            android:label="@string/title_activity_registration_form" />
    </application>

</manifest>